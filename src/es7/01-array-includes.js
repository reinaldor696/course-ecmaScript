//actualizacion javascript junio 2016

//array includes
//nos permite trabajar sobre un array y saber si esta ahi un elemento
// con includes validamos si el valos que colocamos esta dentro del array
let numbers = [1, 2, 3, 4, 5];

console.log(numbers.includes(4));

const list = ['pedro', 'david', 'ana'];
console.log(list.includes('julian'));